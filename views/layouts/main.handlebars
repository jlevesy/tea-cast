<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>TEA Cast</title>
  <style>
    .devices {
      display: grid;
      grid-auto-rows: 50vh;
      grid-template-columns: repeat(2, 1fr);
      height: 100vh;
      width: 100vw;
    }

    .chromecast {
      align-items: center;
      display: flex;
      justify-content: center;
      max-width: 50vw;
      overflow: hidden;
    }

    img {
      max-height: 100%;
      width: auto;
    }
  </style>
</head>
<body>

{{{body}}}

</body>
</html>